namespace: dev
commonLabels:
  app.kubernetes.io/name: "forgerock"

configMapGenerator:
- name: ob-deployment-config

secretGenerator:
- name: ob-secrets

patchesStrategicMerge:
  - configmap.yaml
  - deployment.yaml
  - secret.yaml
  - ingress.yaml

